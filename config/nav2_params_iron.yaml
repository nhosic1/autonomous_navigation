bt_navigator:
  ros__parameters:
    global_frame: map
    robot_base_frame: base_link
    odom_topic: /odometry/filtered
    bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    action_server_result_timeout: 900.0
    navigators: ["navigate_to_pose"]
    navigate_to_pose:
      plugin: "nav2_bt_navigator/NavigateToPoseNavigator"
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_ackermann_back_up_action_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_are_error_codes_active_condition_bt_node
      - nav2_would_a_controller_recovery_help_condition_bt_node
      - nav2_would_a_planner_recovery_help_condition_bt_node
      - nav2_would_a_smoother_recovery_help_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_assisted_teleop_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node
    error_code_names:
      - compute_path_error_code
      - follow_path_error_code

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 1.0
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 4
      height: 4
      resolution: 0.0666667
      footprint: "[ [0.505, 0.31], [0.505, -0.31], [-0.505, -0.31], [-0.505, 0.31] ]"
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: "pointcloud sonar_front_bumper_center sonar_front_bumper_left sonar_front_bumper_right sonar_left_side_bumper sonar_right_side_bumper sonar_rear_bumper_center sonar_rear_bumper_left sonar_rear_bumper_right"
        pointcloud:
          topic: /autonomous_vehicle/stereo/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 2.0
          min_obstacle_height: 0.05
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          clearing: True
          marking: True
        sonar_front_bumper_center:
          topic: /autonomous_vehicle/sonar_front_bumper_center/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_front_bumper_left:
          topic: /autonomous_vehicle/sonar_front_bumper_left/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_front_bumper_right:
          topic: /autonomous_vehicle/sonar_front_bumper_right/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_left_side_bumper:
          topic: /autonomous_vehicle/sonar_left_side_bumper/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_right_side_bumper:
          topic: /autonomous_vehicle/sonar_right_side_bumper/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_center:
          topic: /autonomous_vehicle/sonar_rear_bumper_center/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_left:
          topic: /autonomous_vehicle/sonar_rear_bumper_left/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_right:
          topic: /autonomous_vehicle/sonar_rear_bumper_right/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.55
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: False
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: pointcloud
        pointcloud:
          topic: /autonomous_vehicle/stereo/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 2.0
          min_obstacle_height: 0.05
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          clearing: True
          marking: True
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      always_send_full_costmap: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 10.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_link
      footprint: "[ [0.505, 0.31], [0.505, -0.31], [-0.505, -0.31], [-0.505, 0.31] ]"
      resolution: 0.0666667
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: "stereo_camera sonar_front_bumper_center sonar_front_bumper_left sonar_front_bumper_right sonar_left_side_bumper sonar_right_side_bumper sonar_rear_bumper_center sonar_rear_bumper_left sonar_rear_bumper_right"
        # observation_sources: "stereo_camera"
        stereo_camera:
          topic: /autonomous_vehicle/stereo/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 2.0
          min_obstacle_height: 0.05
          obstacle_max_range: 3.0
          obstacle_min_range: 0.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          clearing: True
          marking: True
        sonar_front_bumper_center:
          topic: /autonomous_vehicle/sonar_front_bumper_center/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_front_bumper_left:
          topic: /autonomous_vehicle/sonar_front_bumper_left/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_front_bumper_right:
          topic: /autonomous_vehicle/sonar_front_bumper_right/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_left_side_bumper:
          topic: /autonomous_vehicle/sonar_left_side_bumper/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_right_side_bumper:
          topic: /autonomous_vehicle/sonar_right_side_bumper/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_center:
          topic: /autonomous_vehicle/sonar_rear_bumper_center/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_left:
          topic: /autonomous_vehicle/sonar_rear_bumper_left/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
        sonar_rear_bumper_right:
          topic: /autonomous_vehicle/sonar_rear_bumper_right/pointcloud
          data_type: PointCloud2
          max_obstacle_height: 0.3
          min_obstacle_height: -0.1
          obstacle_max_range: 2.0
          obstacle_min_range: 0.0
          clearing: False
          marking: True
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.4
      always_send_full_costmap: True

map_saver:
  ros__parameters:
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True

planner_server:
  ros__parameters:
    expected_planner_frequency: 20.0
    planner_plugins: ["GridBased"]

    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid" # In Iron and older versions, "/" was used instead of "::"
      downsample_costmap: false           # whether or not to downsample the map
      downsampling_factor: 1              # multiplier for the resolution of the costmap layer (e.g. 2 on a 5cm costmap would be 10cm)
      tolerance: 0.25                     # dist-to-goal heuristic cost (distance) for valid tolerance endpoints if exact goal cannot be found.
      allow_unknown: true                 # allow traveling in unknown space
      max_iterations: 1000000             # maximum total iterations to search for before failing (in case unreachable), set to -1 to disable
      max_on_approach_iterations: 1000    # Maximum number of iterations after within tolerances to continue to try to find exact solution
      max_planning_time: 5.0              # max time in s for planner to plan, smooth
      motion_model_for_search: "DUBIN"    # Hybrid-A* Dubin, Redds-Shepp
      angle_quantization_bins: 72         # Number of angle bins for search
      analytic_expansion_ratio: 3.5       # The ratio to attempt analytic expansions during search for final approach.
      analytic_expansion_max_length: 3.0  # For Hybrid/Lattice nodes: The maximum length of the analytic expansion to be considered valid to prevent unsafe shortcutting
      analytic_expansion_max_cost: 200.0  # The maximum single cost for any part of an analytic expansion to contain and be valid, except when necessary on approach to goal
      analytic_expansion_max_cost_override: false  #  Whether or not to override the maximum cost setting if within critical distance to goal (ie probably required)
      minimum_turning_radius: 1.097       # minimum turning radius in m of path / vehicle; aligned with controller steering limit
      reverse_penalty: 2.0                # Penalty to apply if motion is reversing, must be => 1
      change_penalty: 0.0                 # Penalty to apply if motion is changing directions (L to R), must be >= 0
      non_straight_penalty: 1.2           # Penalty to apply if motion is non-straight, must be => 1
      cost_penalty: 2.0                   # Penalty to apply to higher cost areas when adding into the obstacle map dynamic programming distance expansion heuristic. This drives the robot more towards the center of passages. A value between 1.3 - 3.5 is reasonable.
      retrospective_penalty: 0.015
      lookup_table_size: 20.0             # Size of the dubin/reeds-sheep distance window to cache, in meters.
      cache_obstacle_heuristic: false     # Cache the obstacle map dynamic programming distance expansion heuristic between subsequent replannings of the same goal location. Dramatically speeds up replanning performance (40x) if costmap is largely static.
      debug_visualizations: false         # For Hybrid nodes: Whether to publish expansions on the /expansions topic as an array of poses (the orientation has no meaning) and the path's footprints on the /planned_footprints topic. WARNING: heavy to compute and to display, for debug only as it degrades the performance.
      use_quadratic_cost_penalty: False
      downsample_obstacle_heuristic: True
      allow_primitive_interpolation: False
      smooth_path: True                   # If true, does a simple and quick smoothing post-processing to the path

      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true
        refinement_num: 2

controller_server:
  ros__parameters:
    odom_topic: /odometry/filtered
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugins: ["progress_checker"]
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]
    use_realtime_priority: false

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0

    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.4
      yaw_goal_tolerance: 0.35            # ~20 degrees

    FollowPath:
      plugin: "nav2_pure_pursuit_controller::Nav2PurePursuitController"
      v_max: 0.9
      ld_min: 1.15
      K_v_turn: 1.3
      K_v_stop: -1.0
      K_ld: 0.5
      ld_goal: 0.75

behavior_server:
  ros__parameters:
    local_costmap_topic: local_costmap/costmap_raw
    global_costmap_topic: global_costmap/costmap_raw
    local_footprint_topic: local_costmap/published_footprint
    global_footprint_topic: global_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["backup", "drive_on_heading", "ackermann_backup", "assisted_teleop", "wait"]
    backup:
      plugin: "nav2_behaviors/BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors/DriveOnHeading"
    ackermann_backup:
      plugin: "autonomous_navigation::AckermannBackUp"
      simulate_ahead_time: 2.0
      forward_probe_time: 6.0
      sample_time_step: 0.1
      reverse_linear_vel: 0.20
      reverse_angular_vel: 0.18
      forward_linear_vel: 0.2
      forward_angular_vel: 0.18
      max_reverse_heading_change_deg: 100.0
      min_reverse_heading_change_deg: 50.0
    wait:
      plugin: "nav2_behaviors/Wait"
    assisted_teleop:
      plugin: "nav2_behaviors/AssistedTeleop"
    local_frame: odom
    global_frame: map
    robot_base_frame: base_link
    transform_tolerance: 0.1
